#includes<stdio.h>
#includes<stdlib.h>
typedef struct stack
{
	int top;
	char s[20];
}stack;
void push(int top,char ch,s[])
{
	top++;
	s[top]=ch;
}
void check()
{
}
main()
{
	stack s;
	char str[30];
	s.top=-1;
	printf("Enter the algebra");
	gets("%s",&str);
	for(int i=0;i<strlen(str);i++)
	{
		
	}
}
