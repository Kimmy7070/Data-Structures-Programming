#include<stdio.h>
int input(arr[],n)
{
    printf("Enter the limit");
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        printf("Enter the %d element",i);
        scanf("%d",&arr[i]);
    }
    return arr[];
}
int main()
{
    int arr[100],n=0;
    input(arr[],n);
    for(int i=0;i<n;i++)
    {
        printf("The %d element",arr[i]);
    }
    return (0);
}